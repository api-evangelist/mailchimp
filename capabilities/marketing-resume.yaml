naftiko: 1.0.0-alpha2
info:
  label: Mailchimp Marketing API  — Resume
  description: 'Mailchimp Marketing API  — Resume. 1 operations. Lead operation: Mailchimp Resume Rss Campaign. Self-contained
    Naftiko capability covering one Mailchimp business surface.'
  tags:
  - Mailchimp
  - Resume
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MAILCHIMP_API_KEY: MAILCHIMP_API_KEY
capability:
  consumes:
  - type: http
    namespace: marketing-resume
    baseUri: https://server.api.mailchimp.com/3.0
    description: Mailchimp Marketing API  — Resume business capability. Self-contained, no shared references.
    resources:
    - name: campaigns-campaign_id-actions-resume
      path: /campaigns/{campaign_id}/actions/resume
      operations:
      - name: postcampaignsidactionsresume
        method: POST
        description: Mailchimp Resume Rss Campaign
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: campaign_id
          in: path
          type: string
          description: The unique id for the campaign.
          required: true
  exposes:
  - type: rest
    namespace: marketing-resume-rest
    port: 8080
    description: REST adapter for Mailchimp Marketing API  — Resume. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/campaigns/{campaign-id}/actions/resume
      name: campaigns-campaign-id-actions-resume
      description: REST surface for campaigns-campaign_id-actions-resume.
      operations:
      - method: POST
        name: postcampaignsidactionsresume
        description: Mailchimp Resume Rss Campaign
        call: marketing-resume.postcampaignsidactionsresume
        with:
          campaign_id: rest.campaign_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: marketing-resume-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mailchimp Marketing API  — Resume. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: mailchimp-resume-rss-campaign
      description: Mailchimp Resume Rss Campaign
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: marketing-resume.postcampaignsidactionsresume
      with:
        campaign_id: tools.campaign_id
      outputParameters:
      - type: object
        mapping: $.
